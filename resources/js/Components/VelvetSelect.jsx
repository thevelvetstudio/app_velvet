export default function VelvetSelect({ label, value, onChange, options, error }) {
    return <label className="block text-xs font-medium text-gray-200">{label}<span className="relative mt-2 block"><select value={value} onChange={onChange} className="velvet-input velvet-select appearance-none pr-10"><option value="">Selecciona una opción</option>{options.map((option) => { const optionValue = typeof option === 'string' ? option : option.value; const optionLabel = typeof option === 'string' ? option : option.label; return <option key={optionValue} value={optionValue}>{optionLabel}</option>; })}</select><span className="pointer-events-none absolute right-4 top-1/2 -translate-y-1/2 text-gray-400">⌄</span></span>{error && <span className="mt-1 block text-xs text-red-400">{error}</span>}</label>;
}
