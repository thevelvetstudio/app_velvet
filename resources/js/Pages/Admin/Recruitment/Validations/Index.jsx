import { Head, Link, router } from '@inertiajs/react';
import { useState } from 'react';
import { FiArrowRight, FiCheckCircle, FiEye, FiImage, FiSearch, FiX } from 'react-icons/fi';
import Layout from '../Layout';

const statusLabels = { NOT_STARTED: 'Sin iniciar', IN_PROGRESS: 'En progreso', IN_REVIEW: 'Revisión manual', APPROVED: 'Aprobada', DECLINED: 'Rechazada', EXPIRED: 'Vencida', ABANDONED: 'Abandonada' };
const statusStyles = { APPROVED: 'border-[#2d8669] bg-[#12372e] text-[#9af2cb]', DECLINED: 'border-[#7d3144] bg-[#321622] text-[#ffb1bd]', IN_REVIEW: 'border-[#80508c] bg-[#321344] text-[#e7b2f1]' };
const formatDate = (value) => value ? new Intl.DateTimeFormat('es-CO', { dateStyle: 'medium', timeStyle: 'short', timeZone: 'America/Bogota' }).format(new Date(value)) : '—';

function Status({ value }) { return <span className={`inline-flex rounded-full border px-3 py-1 text-[11px] ${statusStyles[value] || 'border-[#51445a] bg-[#201b29] text-[#c8bfd0]'}`}>{statusLabels[value] || value}</span>; }

function ImagesGallery({ images = [] }) {
    if (!images.length) return <div className="mt-6 rounded-lg border border-dashed border-[#343044] bg-[#151522] p-5 text-sm text-[#858a99]">Didit no devolvió imágenes visibles para esta sesión o los enlaces ya expiraron.</div>;
    return <div className="mt-6"><div className="flex items-center gap-2"><FiImage className="text-[#d56bea]" /><p className="text-[10px] uppercase tracking-[.16em] text-[#7f8495]">Imágenes de la verificación</p></div><div className="mt-3 grid gap-4 sm:grid-cols-2">{images.map((image) => <a key={`${image.label}-${image.url}`} href={image.url} target="_blank" rel="noreferrer" className="group overflow-hidden rounded-lg border border-[#292d39] bg-[#151522] transition hover:border-[#b12dd5]"><div className="aspect-[4/3] bg-[#0b0c12]"><img src={image.url} alt={image.label} loading="lazy" className="h-full w-full object-contain transition group-hover:scale-[1.02]" /></div><p className="px-3 py-3 text-xs text-[#d7cfe0]">{image.label}<span className="ml-2 text-[#858a99]">↗</span></p></a>)}</div><p className="mt-3 text-[11px] leading-5 text-[#777d8f]">Las imágenes son proporcionadas por Didit y pueden ser temporales. Solo deben consultarse por personal autorizado.</p></div>;
}

export default function Index({ validations, filters }) {
    const [selected, setSelected] = useState(null);
    const search = (event) => router.get('/admin/validations', { ...filters, search: event.target.value }, { preserveState: true, replace: true, preserveScroll: true });
    const filterStatus = (event) => router.get('/admin/validations', { ...filters, status: event.target.value || undefined }, { preserveState: true, replace: true, preserveScroll: true });

    return <><Head title="Validaciones de identidad" /><Layout><div className="mx-auto max-w-[1440px]"><div className="flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p className="text-[10px] uppercase tracking-[.28em] text-[#d56bea]">Onboarding</p><h1 className="mt-2 font-editorial text-4xl text-white sm:text-5xl">Validaciones</h1><p className="mt-2 text-sm text-[#969baa]">Consulta los perfiles verificados por Didit y revisa el resultado de cada sesión.</p></div><div className="flex w-full gap-2 sm:w-auto"><label className="relative flex-1 sm:w-72"><FiSearch className="pointer-events-none absolute left-3 top-1/2 -translate-y-1/2 text-[#777c8e]" size={16} /><input defaultValue={filters.search} onChange={search} placeholder="Buscar nombre, código o email…" className="velvet-input mt-0 pl-10" /></label><select value={filters.status || ''} onChange={filterStatus} className="velvet-dashboard-select"><option value="">Todos</option>{Object.entries(statusLabels).map(([value, label]) => <option key={value} value={value}>{label}</option>)}</select></div></div><div className="mt-8 overflow-hidden rounded-xl border border-[#252936] bg-[#10121a] shadow-[0_18px_55px_rgba(0,0,0,.18)]"><div className="overflow-x-auto"><table className="w-full min-w-[980px] text-left text-sm"><thead className="border-b border-[#282c38] bg-[#141620] text-[10px] uppercase tracking-[.18em] text-[#8e93a5]"><tr><th className="px-5 py-4">Perfil</th><th className="px-5 py-4">Estado Didit</th><th className="px-5 py-4">Mayor de edad</th><th className="px-5 py-4">Fecha validación</th><th className="px-5 py-4">Sesión</th><th className="px-5 py-4 text-right">Acción</th></tr></thead><tbody className="divide-y divide-[#252936]">{validations.data.length ? validations.data.map((item) => <tr key={item.id} className="transition hover:bg-[#171522]"><td className="px-5 py-5"><p className="font-medium text-white">{item.name}</p><p className="mt-1 text-xs text-[#777d8f]">{item.code} · {item.email}</p></td><td className="px-5 py-5"><Status value={item.identity_status} /></td><td className="px-5 py-5 text-xs text-[#c9c2cf]">{item.data?.age_verified ? <span className="inline-flex items-center gap-1 text-[#9af2cb]"><FiCheckCircle />Confirmada</span> : 'No confirmada'}</td><td className="px-5 py-5 text-xs text-[#9297a7]">{formatDate(item.verified_at || item.data?.updated_at)}</td><td className="px-5 py-5 font-mono text-[10px] text-[#777d8f]">{item.session_id}</td><td className="px-5 py-5 text-right"><button type="button" onClick={() => setSelected(item)} className="inline-flex items-center gap-2 rounded-lg border border-[#713080] bg-[#32133d] px-3 py-2 text-xs font-medium text-[#edb4f8] hover:border-[#b12dd5] hover:bg-[#55166e] hover:text-white"><FiEye size={14} />Ver datos</button></td></tr>) : <tr><td colSpan="6" className="px-5 py-16 text-center text-[#777d8f]">No hay verificaciones Didit registradas.</td></tr>}</tbody></table></div></div>{validations.links?.length > 3 && <div className="mt-4 flex flex-wrap gap-2">{validations.links.map((link, index) => <button type="button" key={`${link.label}-${index}`} disabled={!link.url} onClick={() => link.url && router.visit(link.url, { preserveScroll: true })} className={`rounded-lg border px-3 py-2 text-xs ${link.active ? 'border-[#a92ad8] bg-[#55166e] text-white' : 'border-[#343044] text-[#aaa5b5]'} ${!link.url ? 'opacity-40' : 'hover:border-[#a92ad8]'}`} dangerouslySetInnerHTML={{ __html: link.label }} />)}</div>}</div></Layout>{selected && <div className="fixed inset-0 z-[80] grid place-items-center bg-black/75 p-4 backdrop-blur-sm" role="presentation" onMouseDown={(event) => event.target === event.currentTarget && setSelected(null)}><section role="dialog" aria-modal="true" className="max-h-[90vh] w-full max-w-2xl overflow-y-auto rounded-2xl border border-[#4c3157] bg-[#11121a] p-6 shadow-[0_25px_80px_rgba(0,0,0,.6)]"><div className="flex items-start justify-between gap-4"><div><p className="text-[10px] uppercase tracking-[.25em] text-[#d56bea]">Resultado Didit</p><h2 className="mt-2 font-editorial text-3xl text-white">{selected.name}</h2><p className="mt-1 text-xs text-[#858a99]">Sesión {selected.session_id}</p></div><button type="button" onClick={() => setSelected(null)} aria-label="Cerrar" className="rounded-lg p-2 text-[#858a99] hover:bg-[#25152e] hover:text-white"><FiX /></button></div><div className="mt-6 grid gap-4 sm:grid-cols-2"><div className="rounded-lg border border-[#292d39] bg-[#151522] p-4"><p className="text-[10px] uppercase tracking-[.16em] text-[#7f8495]">Estado</p><div className="mt-2"><Status value={selected.identity_status} /></div></div><div className="rounded-lg border border-[#292d39] bg-[#151522] p-4"><p className="text-[10px] uppercase tracking-[.16em] text-[#7f8495]">Mayor de edad</p><p className="mt-2 text-sm text-[#eee8f2]">{selected.data?.age_verified ? 'Confirmada' : 'No confirmada'}</p></div><div className="rounded-lg border border-[#292d39] bg-[#151522] p-4"><p className="text-[10px] uppercase tracking-[.16em] text-[#7f8495]">Nombre del documento</p><p className="mt-2 text-sm text-[#eee8f2]">{selected.data?.name || 'No disponible'}</p></div><div className="rounded-lg border border-[#292d39] bg-[#151522] p-4"><p className="text-[10px] uppercase tracking-[.16em] text-[#7f8495]">Fecha de nacimiento</p><p className="mt-2 text-sm text-[#eee8f2]">{selected.data?.date_of_birth || 'No disponible'}</p></div></div><ImagesGallery images={selected.data?.images} />{selected.failure_reason && <p className="mt-5 rounded-lg border border-[#7d3144] bg-[#321622] px-4 py-3 text-xs leading-5 text-[#ffb1bd]">{selected.failure_reason}</p>}<div className="mt-6 flex justify-end gap-3"><button type="button" onClick={() => setSelected(null)} className="rounded-lg border border-[#343044] px-4 py-2.5 text-xs text-[#c8c2ce]">Cerrar</button><Link href={`/admin/candidates/${selected.id}`} className="velvet-button gap-2">Ver perfil <FiArrowRight /></Link></div></section></div>}</>;
}
