import { Head, Link } from '@inertiajs/react';
import Layout from '../Layout';
export default function Index({ candidates }) { return <><Head title="Candidatos" /><Layout title="Candidatos"><p className="text-gray-600">Personas que avanzaron desde la captación hacia selección.</p><div className="mt-8 overflow-hidden border border-gray-200 bg-white"><table className="w-full text-left text-sm"><thead className="border-b border-gray-200 bg-gray-50 text-xs uppercase tracking-wider text-gray-500"><tr><th className="px-5 py-4">Candidato</th><th className="px-5 py-4">Tipo</th><th className="px-5 py-4">Estado</th><th className="px-5 py-4">Código</th></tr></thead><tbody className="divide-y divide-gray-100">{candidates.data.length ? candidates.data.map(candidate=><tr key={candidate.id}><td className="px-5 py-4"><Link className="font-medium hover:text-[#883499]" href={`/admin/candidates/${candidate.id}`}>{candidate.lead.first_name} {candidate.lead.last_name}</Link><p className="mt-1 text-xs text-gray-500">{candidate.lead.email}</p></td><td className="px-5 py-4">{candidate.candidate_type}</td><td className="px-5 py-4"><span className="rounded-full bg-[#f1e5f4] px-3 py-1 text-xs text-[#883499]">{candidate.status}</span></td><td className="px-5 py-4 font-mono text-xs">{candidate.code}</td></tr>) : <tr><td colSpan="4" className="px-5 py-16 text-center text-gray-500">No hay candidatos todavía.</td></tr>}</tbody></table></div></Layout></>; }
