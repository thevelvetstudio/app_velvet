import { Head, Link } from '@inertiajs/react';
import Layout from '../Layout';
export default function Show({ candidate }) { return <><Head title={candidate.code} /><Layout title="Candidate 360"><Link href="/admin/candidates" className="text-sm text-[#883499]">← Candidatos</Link><section className="mt-8 bg-white p-8"><p className="text-xs uppercase tracking-[.25em] text-[#883499]">{candidate.code}</p><h2 className="mt-3 text-4xl font-semibold">{candidate.lead.first_name} {candidate.lead.last_name}</h2><p className="mt-2 text-gray-500">{candidate.candidate_type} · {candidate.status}</p><div className="mt-10 grid gap-6 border-t border-gray-200 pt-8 sm:grid-cols-3"><div><p className="text-xs uppercase tracking-wider text-gray-400">Email</p><p className="mt-2 text-sm">{candidate.lead.email}</p></div><div><p className="text-xs uppercase tracking-wider text-gray-400">WhatsApp</p><p className="mt-2 text-sm">{candidate.lead.phone}</p></div><div><p className="text-xs uppercase tracking-wider text-gray-400">Ciudad</p><p className="mt-2 text-sm">{candidate.lead.city}</p></div></div><div className="mt-10 border-t border-gray-200 pt-8"><p className="text-xs uppercase tracking-[.25em] text-[#883499]">Actividad</p><div className="mt-5 space-y-4">{candidate.activities.map(item=><div key={item.id} className="border-l-2 border-[#883499] pl-4 text-sm">{item.description}</div>)}</div></div></section></Layout></>; }
